# ScreenAccess Notes

## Done

- Purchased the **ScreenAccess.org** domain.

## To Do

- Build a user-friendly desktop application called **ScreenAccess**, powered by the opesource OpenShare backend.
- Price ScreenAccess at **$5 per device, per year**.
- Set up a **global device-pairing system** for ScreenAccess.
- Allow customers to add an unlimited number of devices, with the annual price increasing by **$5 for each device**.
- Design ScreenAccess to automatically handle the more complicated parts of OpenShare, including:
  - Account creation and management
  - Device pairing
  - Pairing codes
  - Tunnel configuration
  - Off-network connections
- Ensure users never need to interact directly with OpenShare or manually configure networking.
- Use **Cloudflare Tunnel** to provide secure connections between devices on different networks.
- Keep the ScreenAccess experience simple enough that users can install the application, pair their devices, and begin sharing their screens without any technical setup.

# OpenShare Notes

## To Do
- Keep OpenShare opensoure and publish to GitHub
- Make a companion ssystem or lib or something to be able to interface with it through an api
